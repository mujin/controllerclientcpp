set( PACKAGE_VERSION "@MUJINCLIENT_VERSION@" )
if( "${PACKAGE_FIND_VERSION}" VERSION_EQUAL "@MUJINCLIENT_VERSION@")
  set(PACKAGE_VERSION_EXACT 1)
endif()
if( "${PACKAGE_FIND_VERSION_MAJOR}.${PACKAGE_FIND_VERSION_MINOR}" EQUAL "@MUJINCLIENT_SOVERSION@" )
  set(PACKAGE_VERSION_COMPATIBLE 1)
endif()
if( "${PACKAGE_FIND_VERSION_MAJOR}" EQUAL "@MUJINCLIENT_VERSION_MAJOR@" )
  if( "${PACKAGE_FIND_VERSION_MINOR}" LESS_EQUAL "@MUJINCLIENT_VERSION_MINOR@" )
    # user has not specified a minor version, so return compatible
    set(PACKAGE_VERSION_COMPATIBLE 1)
  endif()
endif()
